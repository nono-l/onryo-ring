# 次のセッションへ

このファイルは、何も知らない AI / 人がこのゲームを壊さずに続けるためのメモ。
コメント方針は [showroom-follow-sync](https://github.com/nono-l/showroom-follow-sync) と同じ。
「何をしているか」ではなく **なぜそうしているか・やると壊れること** を書く。

このセッション（2026-09-08）の browser-smoke は
`Failed to load resource: net::ERR_BLOCKED_BY_RESPONSE.NotSameOrigin`
（Google Fonts）で落ちる。`npm run typecheck` と `npm run build` は通っている。
プレビュー本体は動く。フォント CORS を直すなら `__root.tsx` の fonts.googleapis を疑う。
サンドボックス AGENTS.md の「consoleErrors があると smoke 失敗」に引っかかる。

---

## これは何か

『ペタペタペンギン団』の円陣イベント画面を参考にした **別物**。
名前・ペンギン・公式コピーは出さない。タイトルは **怨霊円陣 / ONRYO RING**。
式神はお菊・澪・黒・白面・高天。ボスは花魁。

プレイヤーは円のマスに式神を置き、同じ3体をドラッグ合成する。
手毬（上の列）を壊すと召喚。花魁は寿司レーンをゴールへ進み、着いたら負け。
寿司を倒すと花魁がバック。金皿を倒すとパワーアップ3択。

---

## 絶対に戻さないこと（ユーザーが何度も直させた）

1. **手毬は勝手に落ちない。** `step()` に dropCd で `stack.shift()` して `falling` に入れる処理を二度と足さない。壊したときだけ消えて召喚。
2. **召喚ボタンは無い。** 両を払って出す UI は消した。`trySummon(..., { free: true })` は手毬 pop だけ。
3. **近接の当たりは武器の先端だけ。** 体の円や「穴の中なら口の手毬に当たる」は禁止。`swingTip` / `swingTipR`。見た目の `rotate(swing)` の +Y と一致させる（`cos/sin` で先端を取ると向きがずれる）。
4. **合成は基礎攻撃力と武器の見た目だけ。** レベルで射程・回転速度・攻撃間隔を伸ばさない。回転の速さは `spdMul`（金皿の速度UP）だけ。
5. **レベル1の攻撃力は 1。** `HEROES.*.atk === 1`。表示レベルは `2*level-1`（1/3/5）。
6. **マスは正方形グリッドではない。** 円陣の同心円（1+8+12+16）。`SLOT_LAYOUT`。3x3 に戻さない。6x6 真四角にも戻さない。
7. **ゴールは11時。** 花魁スタートは下付近。近い左下ゴールに戻すな。
8. **花魁 HP は 9999**（wave で 1.38 倍）。199 や 999 に戻すな。
9. **寿司開始 HP は 1。**
10. **金皿3択は Canvas に描け。** React の overlay だとプレビューで出ないことがある。`drawBuffMenu` + `mode === "buff"` + `onPointerDown`。ゲームは `buff` 中は止まる。
11. **デバッグトグルは今だけ右上。** ヒットボックス表示。本番前に外してよい。

---

## 金皿ルール（2026-09-08 時点）

出現:

- `goldKills < 4` … 4皿に1個金（`netaCount % 4 === 0`）
- 4個撃破したあと … **ノーマル10皿ごとに金1**（`netaSinceGold`）
- レーン上の既存皿は出たときの gold フラグのまま。出現レートは **これから出す皿** だけ変わる

HP（新しく出す皿。既存は据え置き）:

- 金皿撃破 0–5: HP 1
- 6個ごと倍を **最初の2回だけ**（6, 12 撃破で x2, x4）
- そのあと（25〜）は **2個撃破ごと** に倍
- 式は `netaHpAt(goldKills)`。wave 倍は掛けない

金皿撃破時:

- `goldKills++`
- ちょうど 4 個目で `netaSinceGold = 0`（10皿カウントを始め直す）
- `onGoldPlate` → `mode = "buff"`、画面中央に3枚。デモ中だけ自動で1枚適用

---

## 戦闘の骨格

- 近接は常時回転。`atkCd <= 0` かつ先端が触れたフレームだけダメージ。届いていなければ CD を回さない（振りが当たるまで待つ）。
- 遠距離は従来どおり弾。
- 合成: 同じ def+level を重ね、合計スタックが3以上で level++。
- 武器見た目スケールは `SWING_REACH`。ヒットも同じ長さ。
- 負け: `boss.track <= 0`（ゴール）。寿司を倒すと `track += 1/BELT_SLOTS`。
- 手毬列は上。奥ほど `temariHpAt(summonCount)` で硬い。召喚コスト上昇と同じ思想。

---

## ファイルを触るとき

- バランス数値は `src/game/data.ts` に集める。
- ルールは `sim.ts`、絵は `draw.ts`。当たりを描画と二重管理しない。先端は `swingTip` を両方から使う。
- 新しい UI を HTML overlay に足すなら、Canvas 側にも同じ操作を置け。このプレビューは overlay が欠けることがある。

## 既知の粗

- smoke の Fonts NotSameOrigin
- 中央の式神は寿司に届きにくい（外周マスへ置くゲーム）
- デバッグスイッチが残っている
- 召喚コスト UI はボタン廃止後も内部値だけ残っている
- wrap に落ちる手毬経路は死んでいるが `falling` 配列は残っている
